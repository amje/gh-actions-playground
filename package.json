{
  "name": "amje-gh-actions-playground",
  "version": "1.3.0",
  "description": "Sandbox for gh actions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "storybook": "mkdir -p storybook-static && touch storybook-static/$(echo $((1 + $RANDOM % 100))).txt"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {}
}
